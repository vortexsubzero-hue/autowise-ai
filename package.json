{
  "name": "autowise-ai",
  "version": "3.0.0",
  "private": true,
  "description": "VIN-powered vehicle dashboard and secure AI Mechanic on Netlify",
  "scripts": {
    "dev": "netlify dev",
    "check": "node --check app.js && node --check netlify/functions/ai-mechanic.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0"
  }
}
